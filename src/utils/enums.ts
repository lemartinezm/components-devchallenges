export enum ButtonVariants {
  default,
  outline,
  text,
}

export enum Colors {
  default,
  primary,
  secondary,
  danger,
}

export enum IconList {
  search,
  home,
  menu,
  close,
  settings,
  done,
  favorite,
  add,
  delete,
}

export enum ButtonSizes {
  sm,
  md,
  lg,
}
